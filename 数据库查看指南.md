# MongoDB 和 Redis 数据查看指南

本文档介绍如何查看 TradingAgents-CN 项目中 MongoDB 和 Redis 的数据。

## 📋 目录

1. [方法一：Web 界面（推荐）](#方法一web-界面推荐)
2. [方法二：命令行工具](#方法二命令行工具)
3. [方法三：GUI 桌面工具](#方法三gui-桌面工具)
4. [方法四：通过 API 查询](#方法四通过-api-查询)

---

## 方法一：Web 界面（推荐）

项目已配置好 Web 管理界面，通过 Docker Compose 启动。

### MongoDB Web 界面 - Mongo Express

#### 1. 启动 Mongo Express

```bash
cd /Users/qiran/LLM_Proj/LS_Proj/zp_ls/TradingAgents-CN

# 启动 Mongo Express
docker-compose up -d mongo-express

# 查看日志
docker-compose logs -f mongo-express
```

#### 2. 访问界面

- **地址**: http://localhost:8082
- **用户名**: `admin`
- **密码**: `tradingagents123`

#### 3. 使用指南

1. 登录后，左侧显示所有数据库（collections）
2. 点击 `tradingagents` 数据库
3. 可以看到所有集合（表）：
   - `users` - 用户表
   - `analysis_tasks` - 分析任务表
   - `stock_basic_info` - 股票信息表
   - 等等

4. 点击集合名称可以：
   - 查看所有数据（默认显示 20 条）
   - 执行查询（使用 MongoDB 查询语法）
   - 添加、编辑、删除文档
   - 查看集合统计信息

#### 4. 常用查询示例

```javascript
// 查询所有用户
{ }

// 查询管理员用户
{ "is_admin": true }

// 查询特定用户
{ "username": "admin" }

// 查询最近的分析任务
{ "status": "completed" }

// 时间范围查询
{
  "created_at": {
    "$gte": { "$date": "2025-02-01" },
    "$lt": { "$date": "2025-02-05" }
  }
}
```

---

### Redis Web 界面 - Redis Commander

#### 1. 启动 Redis Commander

```bash
cd /Users/qiran/LLM_Proj/LS_Proj/zp_ls/TradingAgents-CN

# 启动 Redis Commander
docker-compose up -d redis-commander

# 查看日志
docker-compose logs -f redis-commander
```

#### 2. 访问界面

- **地址**: http://localhost:8081
- **自动连接**: 已配置好连接到本地 Redis

#### 3. 使用指南

1. 登录后，左侧显示 Redis 键列表
2. 可以看到：
   - `cache:stock:000001` - 股票行情缓存
   - `cache:analysis:task_xxx` - 分析任务缓存
   - `session:user_id` - 用户会话
   - `queue:analysis` - 分析任务队列

3. 点击键名可以：
   - 查看值（支持 JSON 格式化）
   - 查看过期时间（TTL）
   - 删除键
   - 添加新键值对

#### 4. 常见键模式

```bash
# 股票行情缓存
cache:stock:{股票代码}
例如: cache:stock:000001

# 分析任务缓存
cache:analysis:{task_id}
例如: cache:analysis:task_12345

# 用户会话
session:{user_id}
例如: session:507f1f77bcf86cd799439011

# 操作日志队列
queue:operation_logs
```

---

## 方法二：命令行工具

### MongoDB 命令行 - mongosh

#### 1. 连接 MongoDB

```bash
# 连接到 MongoDB
mongosh mongodb://admin:tradingagents123@localhost:27017/tradingagents?authSource=admin

# 或简短连接
mongosh mongodb://localhost:27017/tradingagents \
  --username admin \
  --password tradingagents123 \
  --authenticationDatabase admin
```

#### 2. 基本命令

```javascript
// 显示所有数据库
show databases

// 切换到 tradingagents 数据库
use tradingagents

// 显示所有集合（表）
show collections

// 统计集合文档数
db.users.countDocuments()
db.analysis_tasks.countDocuments()

// 查看集合数据
db.users.find()                    // 查看所有用户（前20条）
db.users.find().limit(10)          // 限制显示条数
db.users.find().pretty()           // 格式化输出

// 查询特定数据
db.users.findOne({ "username": "admin" })

// 条件查询
db.analysis_tasks.find({ "status": "completed" })
db.analysis_tasks.find({ "symbol": "000001" })
```

#### 3. 常用查询示例

```javascript
// 1. 查看所有用户
db.users.find({}, {
  username: 1,
  email: 1,
  is_active: 1,
  created_at: 1
})

// 2. 查看管理员用户
db.users.find({ "is_admin": true }).pretty()

// 3. 查看最近的分析任务
db.analysis_tasks.find().sort({ "created_at": -1 }).limit(10).pretty()

// 4. 查看特定用户的自选股
db.users.findOne(
  { "username": "admin" },
  { favorite_stocks: 1 }
)

// 5. 统计分析任务状态
db.analysis_tasks.aggregate([
  { "$group": { "_id": "$status", "count": { "$sum": 1 } } }
])

// 6. 查看失败的的分析任务
db.analysis_tasks.find({
  "status": "failed"
}, {
  task_id: 1,
  symbol: 1,
  last_error: 1,
  created_at: 1
}).pretty()

// 7. 查看股票基本信息
db.stock_basic_info.find({
  "symbol": "000001"
}).pretty()

// 8. 查看今日分析任务
db.analysis_tasks.find({
  "created_at": {
    "$gte": ISODate("2025-02-04T00:00:00Z")
  }
}).count()

// 9. 查看系统配置
db.llm_providers.find().pretty()
db.data_source_configs.find().pretty()

// 10. 查看操作日志
db.operation_logs.find().sort({ "created_at": -1 }).limit(20).pretty()
```

#### 4. 数据修改（谨慎操作）

```javascript
// ⚠️ 谨慎使用！这些操作会修改数据

// 更新用户配额
db.users.updateOne(
  { "username": "admin" },
  { "$set": { "daily_quota": 1000 } }
)

// 添加自选股
db.users.updateOne(
  { "username": "admin" },
  {
    "$push": {
      "favorite_stocks": {
        "stock_code": "000002",
        "stock_name": "万科A",
        "market": "A股",
        "added_at": new Date()
      }
    }
  }
)

// 删除特定任务
db.analysis_tasks.deleteOne({ "task_id": "task_xxx" })
```

---

### Redis 命令行 - redis-cli

#### 1. 连接 Redis

```bash
# 连接 Redis（带密码）
redis-cli -a tradingagents123

# 或简短连接
redis-cli -h localhost -p 6379 -a tradingagents123
```

#### 2. 基本命令

```bash
# 查看所有键
KEYS *

# 查看键的数量
DBSIZE

# 查看键的类型
TYPE cache:stock:000001

# 查看字符串值
GET cache:stock:000001

# 查看哈希值
HGETALL session:user_id

# 查看过期时间（秒）
TTL cache:stock:000001

# 删除键
DEL cache:stock:000001
```

#### 3. 常用查询示例

```bash
# 1. 查看所有股票缓存
KEYS cache:stock:*

# 2. 查看特定股票行情
GET cache:stock:000001
GET cache:stock:600036

# 3. 查看分析任务缓存
KEYS cache:analysis:*
GET cache:analysis:task_12345

# 4. 查看所有用户会话
KEYS session:*

# 5. 查看队列信息
LLEN queue:analysis
LRANGE queue:analysis 0 10

# 6. 查看过期时间
TTL cache:stock:000001
TTL session:507f1f77bcf86cd799439011

# 7. 查看内存使用
INFO memory

# 8. 查看连接信息
INFO clients

# 9. 查看所有数据库
INFO keyspace

# 10. 清空当前数据库（⚠️ 危险操作）
FLUSHDB
```

#### 4. 数据格式化查看

```bash
# Redis 返回的是 JSON 字符串，可以用 Python 格式化查看
GET cache:stock:000001 | python3 -m json.tool

# 或使用 jq 工具
GET cache:stock:000001 | jq '.'
```

---

## 方法三：GUI 桌面工具

### MongoDB GUI 工具

#### 1. MongoDB Compass（官方推荐）

**下载地址**: https://www.mongodb.com/try/download/compass

**连接配置**:
```
连接字符串: mongodb://admin:tradingagents123@localhost:27017/tradingagents?authSource=admin
```

**特点**:
- ✅ 官方工具，完全免费
- ✅ 图形化界面，操作简单
- ✅ 支持查询聚合管道
- ✅ 支持索引管理
- ✅ 性能监控

#### 2. Studio 3T（功能强大）

**下载地址**: https://studio3t.com/

**特点**:
- ✅ 功能最强大
- ✅ 智能查询提示
- ✅ Visual Query Builder
- ❌ 免费版有限制

#### 3. NoSQLBooster（轻量级）

**下载地址**: https://www.nosqlbooster.com/

**特点**:
- ✅ 轻量快速
- ✅ 智能代码补全
- ✅ 查询构建器

---

### Redis GUI 工具

#### 1. RedisInsight（官方推荐）

**下载地址**: https://redis.com/redis-enterprise/redis-insight/

**连接配置**:
```
Host: localhost
Port: 6379
Password: tradingagents123
```

**特点**:
- ✅ 官方工具，完全免费
- ✅ 现代化界面
- ✅ 支持数据可视化
- ✅ 支持命令行模式
- ✅ 性能监控

#### 2. Another Redis Desktop Manager（推荐）

**下载地址**: https://github.com/qishibo/AnotherRedisDesktopManager

**特点**:
- ✅ 开源免费
- ✅ 跨平台（Windows/Mac/Linux）
- ✅ 界面简洁易用
- ✅ 支持多连接

#### 3. Redis Desktop Manager

**下载地址**: https://redisdesktop.com/

**特点**:
- ✅ 经典工具
- ❅ 更新较慢

---

## 方法四：通过 API 查询

您可以编写简单的 API 端点来查询数据库数据。

### 示例：添加数据查询 API

```python
# app/routers/database_debug.py

from fastapi import APIRouter, Depends, HTTPException
from typing import List, Dict, Any
from app.models.user import User
from app.core.auth import get_current_user
from app.core.database import get_database

router = APIRouter(prefix="/api/debug", tags=["Debug"])

@router.get("/collections")
async def list_collections(
    current_user: User = Depends(get_current_user)
):
    """列出所有 MongoDB 集合"""
    db = get_database()
    collections = await db.list_collection_names()
    return {"collections": collections}

@router.get("/collections/{collection_name}")
async def get_collection_data(
    collection_name: str,
    limit: int = 20,
    current_user: User = Depends(get_current_user)
):
    """获取集合数据"""
    db = get_database()
    collection = db[collection_name]

    cursor = collection.find().limit(limit)
    documents = await cursor.to_list(length=limit)

    # 转换 ObjectId 为字符串
    from bson import json_util
    return json_util.dumps(documents)

@router.get("/redis/keys")
async def list_redis_keys(
    pattern: str = "*",
    current_user: User = Depends(get_current_user)
):
    """列出 Redis 键"""
    import redis
    from app.core.config import settings

    r = redis.Redis(
        host=settings.REDIS_HOST,
        port=settings.REDIS_PORT,
        password=settings.REDIS_PASSWORD,
        db=settings.REDIS_DB,
        decode_responses=True
    )

    keys = r.keys(pattern)
    return {"keys": keys, "count": len(keys)}

@router.get("/redis/key/{key_name}")
async def get_redis_key(
    key_name: str,
    current_user: User = Depends(get_current_user)
):
    """获取 Redis 键值"""
    import redis
    from app.core.config import settings

    r = redis.Redis(
        host=settings.REDIS_HOST,
        port=settings.REDIS_PORT,
        password=settings.REDIS_PASSWORD,
        db=settings.REDIS_DB,
        decode_responses=True
    )

    key_type = r.type(key_name).decode()
    value = None

    if key_type == "string":
        value = r.get(key_name)
    elif key_type == "hash":
        value = r.hgetall(key_name)
    elif key_type == "list":
        value = r.lrange(key_name, 0, -1)
    elif key_type == "set":
        value = r.smembers(key_name)
    elif key_type == "zset":
        value = r.zrange(key_name, 0, -1, withscores=True)

    ttl = r.ttl(key_name)

    return {
        "key": key_name,
        "type": key_type,
        "value": value,
        "ttl": ttl
    }
```

### 在 main.py 中注册路由

```python
from app.routers import database_debug

app.include_router(database_debug.router)
```

### 使用 API 查询数据

```bash
# 1. 列出所有集合
curl http://localhost:8000/api/debug/collections \
  -H "Authorization: Bearer YOUR_TOKEN"

# 2. 获取用户表数据
curl http://localhost:8000/api/debug/collections/users?limit=10 \
  -H "Authorization: Bearer YOUR_TOKEN"

# 3. 列出 Redis 键
curl http://localhost:8000/api/debug/redis/keys?pattern=cache:* \
  -H "Authorization: Bearer YOUR_TOKEN"

# 4. 获取特定键值
curl http://localhost:8000/api/debug/redis/key/cache:stock:000001 \
  -H "Authorization: Bearer YOUR_TOKEN"
```

---

## 🎯 推荐方案总结

### 开发调试（推荐）

| 场景 | 推荐工具 | 原因 |
|------|---------|------|
| **快速查看** | Web 界面（Mongo Express + Redis Commander） | 无需安装，浏览器访问 |
| **日常开发** | 命令行工具（mongosh + redis-cli） | 快速、灵活、强大 |
| **数据分析** | MongoDB Compass + RedisInsight | 可视化、功能强大 |
| **批量操作** | 编写 API 或脚本 | 灵活、可复用 |

### 生产环境（推荐）

| 场景 | 推荐工具 | 原因 |
|------|---------|------|
| **监控** | RedisInsight + MongoDB Atlas | 性能监控、告警 |
| **运维** | 命令行工具 | 安全、高效 |
| **备份恢复** | mongodump + mongorestore | 官方工具 |

---

## ⚠️ 注意事项

### 安全提示

1. **不要在生产环境使用 Web 界面**：
   - 容易暴露敏感数据
   - 缺少审计日志

2. **谨慎使用删除操作**：
   ```javascript
   // MongoDB: 删除前先查询确认
   db.analysis_tasks.find({ "task_id": "xxx" })
   db.analysis_tasks.deleteOne({ "task_id": "xxx" })
   ```

3. **备份重要数据**：
   ```bash
   # 备份 MongoDB
   mongodump mongodb://admin:tradingagents123@localhost:27017/tradingagents \
     --authenticationDatabase admin \
     --out ./backup

   # 备份 Redis
   redis-cli --rdb ./backup/dump.rdb
   ```

### 性能提示

1. **避免全表扫描**：
   ```javascript
   // ❌ 不推荐
   db.users.find()

   // ✅ 推荐
   db.users.find({ "is_active": true }).limit(100)
   ```

2. **使用索引字段查询**：
   ```javascript
   // 使用索引字段（username、email 等）
   db.users.find({ "username": "admin" })
   ```

3. **Redis 大键问题**：
   ```bash
   # 查看大键
   redis-cli --bigkeys

   # 避免存储过大的值
   ```

---

## 📚 学习资源

### MongoDB
- 官方文档: https://docs.mongodb.com/
- MongoDB 大学: https://university.mongodb.com/
- 查询语法: https://docs.mongodb.com/manual/tutorial/query-documents/

### Redis
- 官方文档: https://redis.io/docs/
- 命令参考: https://redis.io/commands/
- Redis 最佳实践: https://redis.io/docs/manual/patterns/

---

**现在选择您喜欢的方式开始查看数据库吧！** 🚀
