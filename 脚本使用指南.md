# 服务管理脚本使用指南

## 📋 可用脚本

### 1. `./stop.sh` - 停止所有服务

**功能:**
- 停止前端和后端进程
- 等待进程完全结束
- **清除Python缓存** (确保代码修改生效)
- 检查端口占用
- 可选停止数据库服务

**使用方法:**
```bash
./stop.sh
```

**执行流程:**
1. 停止前端和后端进程
2. 等待3秒确保进程完全结束
3. 强制清除残留进程
4. **清除所有Python缓存** (`__pycache__` 和 `.pyc` 文件)
5. 检查端口占用情况

**输出示例:**
```
==================================
🛑 TradingAgents-CN 停止中...
==================================

🔍 [1/3] 查找并停止应用进程...
⚙️  停止后端服务 (PID: 12345)...
✅ 后端服务已停止

⏳ [3/4] 等待进程完全结束...
✅ 所有进程已完全结束

🧹 [4/4] 清除Python缓存...
   发现 25 个缓存目录和 156 个缓存文件
✅ Python缓存已彻底清除

🔍 [5/5] 检查端口占用...
✅ 所有端口已释放
```

---

### 2. `./restart_backend.sh` - 重启后端服务

**功能:**
- 停止后端进程
- 释放8000端口
- **清除Python缓存**
- 验证代码修改是否生效
- 重新启动后端

**使用方法:**
```bash
./restart_backend.sh
```

**执行流程:**
1. 停止后端进程(强制停止残留进程)
2. 等待端口释放(强制清理占用)
3. 清除Python缓存
4. 验证关键代码修改
5. 启动后端服务(前台运行,可直接看到日志)

**输出示例:**
```
==================================
🔄 重启后端服务
==================================

📛 [1/5] 停止后端进程...
   发现运行中的后端进程 (PID: 12345)
   ✅ 后端进程已停止

⏳ [2/5] 等待端口释放...
   ✅ 端口已释放

🧹 [3/5] 清除Python缓存...
   发现 25 个缓存目录
   ✅ Python缓存已清除

🔍 [4/5] 验证关键代码修改...
   ✅ backtest_engine.py 代码已更新
   ✅ auth_db.py 代码已更新

🚀 [5/5] 启动后端服务...
   启动时间: 2026-02-09 20:35:00

[后端启动日志...]
```

---

### 3. `./start.sh` - 启动所有服务

**功能:**
- 启动数据库服务(如果未运行)
- 启动后端服务
- 启动前端服务

**使用方法:**
```bash
./start.sh
```

---

## 🎯 推荐使用场景

### 场景1: 修改了Python代码后

**使用: `./restart_backend.sh`**

**原因:**
- 自动清除Python缓存
- 确保代码修改生效
- 验证关键代码是否存在

---

### 场景2: 完全停止所有服务

**使用: `./stop.sh`**

**原因:**
- 彻底清除缓存
- 释放所有端口
- 为下次启动做准备

---

### 场景3: 后端出现问题需要重启

**使用: `./restart_backend.sh`**

**原因:**
- 强制停止残留进程
- 清理端口占用
- 从干净状态启动

---

## ⚠️ 重要提示

### Python缓存问题

**问题表现:**
- 修改了代码,但运行时没有生效
- 日志中看不到添加的调试信息
- 变量值仍然是旧代码的值

**解决方法:**
```bash
# 方法1: 使用 stop.sh (推荐)
./stop.sh

# 方法2: 使用 restart_backend.sh
./restart_backend.sh

# 方法3: 手动清除
find app -type d -name "__pycache__" -exec rm -rf {} +
find app -name "*.pyc" -delete
```

---

### 端口占用问题

**问题表现:**
```
[Errno 48] error while attempting to bind on address ('0.0.0.0', 8000): 
address already in use
```

**解决方法:**
```bash
# 方法1: 使用 restart_backend.sh (自动处理)
./restart_backend.sh

# 方法2: 手动清理
lsof -ti:8000 | xargs kill -9
```

---

## 📊 脚本对比

| 脚本 | 停止服务 | 清除缓存 | 启动服务 | 适用场景 |
|------|---------|---------|---------|---------|
| `stop.sh` | ✅ | ✅ | ❌ | 完全停止 |
| `restart_backend.sh` | ✅ | ✅ | ✅ | 重启后端 |
| `start.sh` | ❌ | ❌ | ✅ | 首次启动 |

---

## 🔧 自定义配置

### 修改项目路径

如果你的项目路径不是 `/Users/qiran/LLM_Proj/LS_Proj/zp_ls/TradingAgents-CN`,请修改:

**stop.sh:**
```bash
PROJECT_DIR="/your/path/to/TradingAgents-CN"
```

**restart_backend.sh:**
```bash
PROJECT_DIR="/your/path/to/TradingAgents-CN"
```

---

## 📝 故障排查

### 问题1: 脚本没有执行权限

**解决:**
```bash
chmod +x stop.sh restart_backend.sh start.sh
```

### 问题2: 找不到 Python 命令

**解决:**
```bash
# 使用虚拟环境的 Python
./venv/bin/python3 -m app
```

### 问题3: 缓存清除失败

**解决:**
```bash
# 手动清除
sudo find app -type d -name "__pycache__" -exec rm -rf {} +
sudo find app -name "*.pyc" -delete
```

---

## 💡 最佳实践

1. **修改代码后**: 始终使用 `./restart_backend.sh` 重启
2. **遇到怪异问题**: 先执行 `./stop.sh` 清除缓存
3. **查看启动日志**: `./restart_backend.sh` 在前台运行,可直接看到日志
4. **验证修改**: 脚本会自动验证关键代码是否存在

---

**更新时间:** 2026-02-09  
**版本:** v1.0
