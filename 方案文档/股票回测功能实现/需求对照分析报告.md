# 股票回测功能需求对照分析报告

生成时间：2026-02-07
对比基准：`./需求文档/股票回测功能实现.md`

---

## 📊 总体完成情况

| 需求模块 | 实现状态 | 符合度 | 说明 |
|---------|---------|--------|------|
| 界面基础功能 | ✅ 已实现 | 70% | 基本布局完成，但缺少部分交互细节 |
| 回测参数设置 | ✅ 已实现 | 85% | 核心参数支持，但缺少参数保存功能 |
| 策略选择功能 | ✅ 已实现 | 90% | 策略列表和选择功能完整 |
| 回测执行引擎 | ✅ 已实现 | 95% | 核心逻辑完整，支持中断/继续 |
| 进度管理 | ✅ 已实现 | 100% | 实时进度展示完整 |
| 结果计算与展示 | ✅ 已实现 | 90% | 核心指标完整，可视化图表完善 |
| 历史记录管理 | ✅ 已实现 | 85% | 基本CRUD完整，缺少对比功能 |
| 导出服务 | ✅ 已实现 | 100% | Excel导出功能完整 |

**总体符合度：89%**

---

## ❌ 不符合需求的地方（按优先级排序）

### P0 级别（核心功能缺失）

#### 1. 页面布局与导航结构不符合需求

**需求描述（3.2节）：**
> 功能入口设置：股票回测功能入口位于系统左侧边框导航栏，属于一级导航item，导航栏整体固定在页面左侧，在一级导航item"模拟交易"的下面。入口名称明确标注为"股票回测"，搭配股票图标（简约线条风格）。该一级item有二级子菜单"执行股票回测"、"回测策略列表"、"历史回测汇总"。

**当前实现：**
- ❌ 一级导航名称为"回测管理"（应为"股票回测"或"模拟交易"）
- ❌ 二级菜单为"回测控制"、"策略列表"、"历史记录"（名称不对应）
- ❌ 缺少"股票图标"（简约线条风格）

**影响范围：**
- 前端路由配置：`frontend/src/router/index.ts:206-230`
- 侧边栏菜单配置

**建议修复：**
1. 修改路由配置，将"回测管理"改为"股票回测"或其父级"模拟交易"
2. 调整二级菜单名称为"执行股票回测"、"回测策略列表"、"历史回测汇总"
3. 添加股票图标（使用 Element Plus 的 TrendCharts 或自定义 SVG）

---

#### 2. 缺少"重置所有参数"和"我的常用参数"快捷按钮

**需求描述（3.2节）：**
> 右侧设置两个快捷按钮，分别为"重置所有参数"（点击后恢复所有参数为默认值，含回测参数、策略参数）和"我的常用参数"（下拉菜单，显示用户保存的常用参数组，支持快速调用）

**当前实现：**
- ❌ BacktestControlPanel.vue 顶部区域缺少这两个按钮
- ❌ 参数保存功能未实现（需求3.3节明确要求）

**影响范围：**
- `frontend/src/views/Backtest/BacktestControlPanel.vue`

**建议修复：**
1. 在页面顶部区域添加"重置所有参数"按钮
2. 实现"我的常用参数"下拉菜单及保存参数功能
3. 后端需要支持常用参数的CRUD操作（新增API端点）

---

#### 3. 缺少"操作步骤指引"和"当前状态提示"左侧辅助栏

**需求描述（3.2节）：**
> 左侧辅助信息栏：固定显示"操作步骤指引"和"当前状态提示"。操作步骤指引按顺序列出回测全流程（1. 回测参数设置→2. 策略选择→3. 触发回测→4. 查看结果），当前所处步骤高亮显示

**当前实现：**
- ❌ 页面布局为单栏设计，缺少左侧辅助信息栏
- ❌ 缺少操作步骤指引组件
- ❌ 缺少状态提示（如"未开始回测""回测参数已确认，待选择策略"）

**影响范围：**
- `frontend/src/views/Backtest/BacktestControlPanel.vue`

**建议修复：**
1. 改造页面布局为"左2右8"分栏布局
2. 实现操作步骤指引组件，支持高亮当前步骤
3. 实现状态提示组件，根据回测流程动态显示状态

---

#### 4. 回测参数设置缺少"股票最小购买量"字段

**需求描述（3.3节）：**
> 股票最小购买量设置：支持用户人工填写股票最小交易数量（买入、卖出通用），明确所有A股、港股交易股数，必须是100的倍数，设置数量范围约束（100股-10000股）

**当前实现：**
- ✅ 后端回测引擎支持该参数
- ❌ 前端 BacktestControlPanel.vue 缺少该输入字段
- ❌ 缺少"100的倍数"验证逻辑

**影响范围：**
- `frontend/src/views/Backtest/BacktestControlPanel.vue`
- 表单验证逻辑

**建议修复：**
1. 在表单中添加"股票最小购买量"输入框
2. 添加验证规则：必须是100的倍数，范围100-10000
3. 调整提示文案：支持100的倍数说明

---

#### 5. 缺少帮助文档入口

**需求描述（3.2节）：**
> 底部区域（固定不滚动）：左侧设置"帮助文档入口"（文字+问号图标），点击后弹出新窗口展示帮助文档

**当前实现：**
- ❌ 页面底部区域未实现
- ❌ 缺少帮助文档入口

**影响范围：**
- `frontend/src/views/Backtest/BacktestControlPanel.vue`

**建议修复：**
1. 在页面底部添加固定区域
2. 添加"帮助文档"链接，指向使用指南

---

### P1 级别（重要功能缺失）

#### 6. 历史记录对比功能未实现

**需求描述（3.7节）：**
> 记录对比：支持用户选择最多5条历史记录进行对比，以表格+图表形式展示核心数据对比（初始资金、最小购买量、总收益率、年化收益率、最大回撤、交易次数）及资金曲线对比

**当前实现：**
- ✅ 历史记录列表完整
- ✅ 支持单个记录查看
- ❌ 缺少多选对比功能
- ❌ 缺少对比视图（表格+图表）

**影响范围：**
- `frontend/src/views/Backtest/BacktestHistory.vue`

**建议修复：**
1. 实现多选复选框
2. 实现"对比"按钮
3. 创建对比视图页面/弹窗
4. 后端需要支持批量数据查询API

---

#### 7. 历史记录检索功能不完整

**需求描述（3.7节）：**
> 检索功能：支持多条件检索，检索条件包括策略名称、回测时间区间、初始资金范围、股票最小购买量、最终收益率范围

**当前实现：**
- ✅ 支持搜索（记录名称或描述）
- ✅ 支持策略筛选
- ✅ 支持股票代码筛选
- ❌ 缺少回测时间区间筛选
- ❌ 缺少初始资金范围筛选
- ❌ 缺少最小购买量筛选
- ❌ 缺少最终收益率范围筛选

**影响范围：**
- `frontend/src/views/Backtest/BacktestHistory.vue`
- 后端检索API

**建议修复：**
1. 前端添加更多筛选条件
2. 后端支持范围查询（如资金范围、收益率范围）
3. 优化检索性能（添加索引）

---

#### 8. 回测结果展示区缺少"回测参数"和"策略参数"展示

**需求描述（3.6节）：**
> 基础信息：回测时间区间、初始资金、股票最小购买量、所选策略名称及参数、回测总时长（交易日数量）、交易次数

**当前实现：**
- ✅ 展示核心指标（收益率、回撤、胜率等）
- ❌ BacktestResults.vue 未明确展示回测参数（如最小购买量）
- ❌ 策略参数展示不清晰

**影响范围：**
- `frontend/src/components/BacktestResults.vue`

**建议修复：**
1. 在结果展示区添加"回测参数"卡片
2. 明确展示策略名称及参数
3. 展示股票最小购买量

---

#### 9. 交易明细展示不完整

**需求描述（3.6节）：**
> 交易明细：支持用户点击"查看详情"，查看每一笔模拟交易的完整明细，包含交易日期、交易类型（买入/卖出）、股票代码、股票名称、交易价格、交易数量、交易金额、交易时可用现金、交易后持仓数量、是否符合"100的倍数"交易规则、是否按金额关联规则调整、盈亏金额

**当前实现：**
- ✅ 支持交易明细查看
- ✅ 展示基础字段（日期、类型、价格、数量）
- ❌ 缺少"交易时可用现金"字段
- ❌ 缺少"交易后持仓数量"字段
- ❌ 缺少"是否符合100的倍数"标识
- ❌ 缺少"盈亏金额"字段

**影响范围：**
- `frontend/src/components/BacktestResults.vue`
- 后端数据结构

**建议修复：**
1. 后端确保返回完整字段
2. 前端表格添加缺失列
3. 添加标识列（如是否符合100倍数）

---

### P2 级别（体验优化）

#### 10. 缺少新手指引弹窗

**需求描述（3.5节）：**
> 新手指引：首次使用的用户，点击"回测"按钮后，先弹出简易操作指引弹窗（3步核心操作），点击"我知道了"后再显示参数确认弹窗

**当前实现：**
- ❌ 无新手指引功能

**建议修复：**
1. 检测用户首次使用（localStorage记录）
2. 实现指引弹窗组件
3. 3步指引：参数设置 → 策略选择 → 触发回测

---

#### 11. 缺少快捷键支持

**需求描述（4.1节）：**
> 操作便捷性：策略选择、参数设置、回测触发等核心操作，支持快捷键（如Ctrl+S保存参数、Ctrl+R触发回测）

**当前实现：**
- ❌ 无快捷键支持

**建议修复：**
1. 添加全局快捷键监听
2. Ctrl+S: 保存参数
3. Ctrl+R: 触发回测
4. Esc: 取消/关闭弹窗

---

#### 12. 缺少参数确认弹窗

**需求描述（3.5节）：**
> 若参数均合法，弹出确认弹窗，显示当前所有回测参数（时间区间、初始资金、最小购买量、策略名称及参数）

**当前实现：**
- ❌ 点击"启动回测"直接执行，无确认弹窗

**建议修复：**
1. 实现参数确认弹窗组件
2. 展示所有参数摘要
3. 用户确认后才触发回测

---

#### 13. 进度展示不够详细

**需求描述（3.5节）：**
> 进度展示：回测执行过程中，界面显示回测进度（按交易日数量计算，如"回测中，当前进度60%（已完成223/372个交易日）"）、当前执行日期、当前可用现金、当前持仓概况

**当前实现：**
- ✅ 显示进度百分比
- ✅ 显示当前日期
- ⚠️ 可用现金和持仓概况展示不够突出

**建议修复：**
1. 增强进度信息展示
2. 明确显示"已完成X/Y个交易日"
3. 突出显示当前资金和持仓

---

#### 14. 回测结果导出Excel格式不符合需求

**需求描述（3.6节）：**
> 导出字段：Excel文件包含3个工作表，分别为"回测参数"、"核心结果"、"交易明细"

**当前实现：**
- ✅ 支持Excel导出
- ⚠️ 工作表名称为"回测摘要"、"详细指标"、"交易明细"、"资金曲线"（4个工作表）
- ❌ 未按需求的3个工作表组织

**影响范围：**
- `app/services/export_service.py`

**建议修复：**
1. 调整工作表结构为3个：
   - "回测参数"：时间区间、初始资金、最小购买量、策略名称及参数
   - "核心结果"：盈利数据、风险数据、基础信息
   - "交易明细"：完整交易记录

---

#### 15. 回测历史记录上限未强制执行

**需求描述（3.6节）：**
> 记录上限：回测历史记录最多保存100条，超出后自动删除最早的记录，删除前弹出提示

**当前实现：**
- ✅ 支持历史记录保存
- ❌ 未实现100条上限检查
- ❌ 未实现自动删除最早记录

**影响范围：**
- `app/services/history_service.py`

**建议修复：**
1. 保存历史记录前检查数量
2. 如果达到100条，提示用户
3. 用户确认后删除最早记录

---

#### 16. 删除记录后无恢复功能

**需求描述（3.7节）：**
> 恢复功能：删除的历史记录进入回收站，保留7天，7天内用户可在回收站找回

**当前实现：**
- ✅ 已实现回收站功能
- ✅ 支持软删除
- ⚠️ 需确认7天自动清理机制是否实现

**建议检查：**
1. 检查后端定时任务是否实现7天清理
2. 检查前端回收站视图是否完整

---

## ⚠️ 部分符合需求的地方

#### 1. 模拟持仓/现金预览区

**需求描述（3.2节）：**
> 模拟持仓/现金预览区：贯穿回测全流程，回测前展示初始资金（用户设置值）、初始持仓（默认空仓，显示"无持仓"）；回测中实时展示当前回测日期、可用现金、持仓股票明细

**当前实现：**
- ✅ BacktestControlPanel.vue 包含持仓展示
- ⚠️ 布局位置：需求要求位于"回测操作区右侧"，当前实现位置可能不符
- ⚠️ 需确认是否按卡片式设计

---

#### 2. 回测结果展示区布局

**需求描述（3.2节）：**
> 上方为核心结果概览（横向排列核心数据卡片），下方为详情查看区（左侧为交易明细表格，右侧为可视化图表）

**当前实现：**
- ✅ BacktestResults.vue 包含核心指标和图表
- ⚠️ 需确认布局是否符合"横向排列卡片"和"左表右图"

---

## ✅ 完全符合需求的部分

1. **策略管理服务**：策略注册、编辑、删除、列表展示功能完整
2. **交易日历服务**：A股交易日识别功能完整
3. **股票数据服务**：日级别数据获取、缓存、多数据源降级功能完整
4. **回测执行引擎**：T+1规则、100倍数规则、资金持仓同步逻辑完整
5. **进度管理服务**：实时进度推送、WebSocket更新功能完整
6. **结果计算服务**：收益率、回撤、夏普比率等指标计算完整
7. **导出服务**：Excel文件生成、样式美化功能完整
8. **历史记录基础CRUD**：查询、保存、删除、回收站功能完整

---

## 📋 待开发功能清单

### 核心功能（P0-P1）
1. 修改前端路由和侧边栏导航名称
2. 实现"重置所有参数"按钮
3. 实现"我的常用参数"功能（含后端API）
4. 改造页面布局为"左2右8"，添加操作步骤指引
5. 添加"股票最小购买量"表单字段
6. 实现历史记录对比功能
7. 完善历史记录检索条件
8. 补充交易明细缺失字段
9. 实现参数确认弹窗
10. 调整Excel导出格式为3个工作表

### 体验优化（P2）
11. 实现新手指引弹窗
12. 添加快捷键支持
13. 实现历史记录100条上限检查
14. 确认7天自动清理机制

---

## 📊 修复优先级建议

### 第一批（核心交互）
- 导航名称修正
- 页面布局改造（左2右8）
- 操作步骤指引组件
- 重置参数按钮
- 参数确认弹窗

### 第二批（功能完整性）
- 我的常用参数功能
- 股票最小购买量字段
- 历史记录对比
- 完善检索条件
- 交易明细字段补充

### 第三批（体验优化）
- 新手指引
- 快捷键
- Excel格式调整
- 上限检查

---

## 💡 技术债务

1. **前端组件复用性**：操作步骤指引、状态提示可抽取为通用组件
2. **参数管理**：常用参数需要独立的数据模型和API设计
3. **对比功能**：可能需要后端支持批量查询以优化性能
4. **历史记录上限**：建议使用数据库触发器或应用层定时任务

---

**报告生成时间**：2026-02-07
**分析人员**：AI 代码审查
**下次审查建议**：完成第一批修复后重新对照需求
