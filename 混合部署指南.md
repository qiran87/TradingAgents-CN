# TradingAgents-CN 混合部署指南

## 📋 部署方案说明

本指南采用**混合部署方案**，兼顾开发便利性和环境隔离：
- **数据库服务（MongoDB + Redis）**：使用 Docker 运行
- **应用服务（后端 + 前端）**：本地源码运行，方便调试

## 🚀 快速开始

### 第一步：安装 Docker Desktop

#### 1. 下载 Docker Desktop for Mac (Intel)

**方式一：通过浏览器下载（推荐）**
1. 访问官网：https://www.docker.com/products/docker-desktop/
2. 点击 "Download for Mac - Chip: Intel"
3. 下载完成后，双击 `Docker.dmg` 文件
4. 将 Docker 拖拽到 Applications 文件夹

**方式二：通过命令行下载**
```bash
cd ~/Downloads
curl -L -o Docker.dmg https://desktop.docker.com/mac/main/amd64/Docker.dmg
hdiutil attach Docker.dmg
cp -r /Volumes/Docker/Docker.app /Applications/
hdiutil detach /Volumes/Docker
```

#### 2. 启动 Docker Desktop

1. 打开 Applications 文件夹
2. 双击 `Docker.app` 图标
3. 首次启动会要求输入管理员密码
4. 等待 Docker 引擎启动（菜单栏出现 Docker 图标）

#### 3. 验证 Docker 安装

```bash
docker --version
docker-compose --version
```

### 第二步：启动数据库服务

  安装 Docker 后，使用我创建的便捷脚本：

  cd /Users/qiran/LLM_Proj/LS_Proj/zp_ls/TradingAgents-CN

  # 一键启动所有服务（数据库 + 后端 + 前端）
  ./start.sh

  或者手动启动：

  # 1. 启动数据库（Docker）
  docker-compose up -d mongodb redis

  # 2. 初始化数据库（重要！）
  source venv/bin/activate
  python scripts/import_config_and_create_user.py --host

  # 3. 启动后端（终端1）
  source venv/bin/activate
  python -m app

  # 4. 启动前端（终端2）
  cd frontend
  npm run dev


```bash
# 进入项目目录
cd /Users/qiran/LLM_Proj/LS_Proj/zp_ls/TradingAgents-CN

# 仅启动 MongoDB 和 Redis（使用项目提供的 docker-compose.yml）
docker-compose up -d mongodb redis

# 查看数据库状态
docker-compose ps

# 查看数据库日志
docker-compose logs -f mongodb redis
```

**数据库默认账号：**
- MongoDB:
  - 用户名: `admin`
  - 密码: `tradingagents123`
  - 数据库: `tradingagents`
- Redis:
  - 密码: `tradingagents123`

### 第三步：初始化数据库

```bash
# 激活 Python 虚拟环境
cd /Users/qiran/LLM_Proj/LS_Proj/zp_ls/TradingAgents-CN
source venv/bin/activate

# 运行数据库初始化脚本（重要！）
python scripts/import_config_and_create_user.py --host
```

**⚠️ 注意：此步骤必须执行！**
- 导入系统配置数据到 MongoDB
- 创建默认管理员用户（用户名：`admin`，密码：`admin123`）
- 初始化 LLM 提供商、市场分类等基础数据

### 第四步：启动后端服务

```bash
# 确保虚拟环境已激活
source venv/bin/activate

# 启动后端服务
python -m app
```

后端服务将在 http://localhost:8000 启动

**验证后端：**
- API 文档：http://localhost:8000/docs
- 健康检查：http://localhost:8000/api/health

### 第五步：启动前端服务

**打开新的终端窗口：**

```bash
# 进入前端目录
cd /Users/qiran/LLM_Proj/LS_Proj/zp_ls/TradingAgents-CN/frontend

# 启动前端开发服务器
npm run dev
```

前端服务将在 http://localhost:3000 或 http://localhost:5173 启动

## 🎯 部署验证

### 1. 检查数据库状态
```bash
docker-compose ps mongodb redis
# 应该显示 Status 为 Up
```

### 2. 检查后端服务
访问：http://localhost:8000/api/health
应该返回：`{"status":"healthy","timestamp":"..."}`

### 3. 测试登录
访问：http://localhost:3000
- 用户名：`admin`
- 密码：`admin123`

## 🔄 日常使用

### 启动服务

**启动顺序很重要：**

1. **启动 Docker（如果未运行）**
   - 打开 Docker Desktop 应用

2. **启动数据库**
   ```bash
   cd /Users/qiran/LLM_Proj/LS_Proj/zp_ls/TradingAgents-CN
   docker-compose up -d mongodb redis
   ```

3. **启动后端**（终端1）
   ```bash
   cd /Users/qiran/LLM_Proj/LS_Proj/zp_ls/TradingAgents-CN
   source venv/bin/activate
   python -m app
   ```

4. **启动前端**（终端2）
   ```bash
   cd /Users/qiran/LLM_Proj/LS_Proj/zp_ls/TradingAgents-CN/frontend
   npm run dev
   ```

### 关闭服务

**关闭顺序：**

1. **停止前端**：在终端2 按 `Ctrl + C`
2. **停止后端**：在终端1 按 `Ctrl + C`
3. **停止数据库**（可选）：
   ```bash
   docker-compose stop mongodb redis
   ```
   或保留数据库运行，下次启动更快

## 💡 开发调试技巧

### 后端调试
- 直接修改 Python 代码
- 重启后端服务（Ctrl + C 后重新运行 `python -m app`）
- 使用 IDE（如 PyCharm、VSCode）的调试功能

### 前端调试
- 前端支持热重载，修改代码自动刷新
- 打开浏览器开发者工具（F12）查看调试信息
- Vue DevTools 扩展推荐安装

### 数据库管理
- 查看 MongoDB 日志：`docker-compose logs -f mongodb`
- 查看 Redis 日志：`docker-compose logs -f redis`
- 重启数据库：`docker-compose restart mongodb redis`

### 清理环境
```bash
# 停止所有服务
docker-compose down

# 停止并删除数据卷（⚠️ 会删除所有数据）
docker-compose down -v
```

## 🔧 常见问题

### 1. Docker Desktop 无法启动
- 确保系统版本兼容（macOS 10.15+）
- 检查是否有足够的磁盘空间
- 尝试重启系统后再次启动

### 2. 端口被占用
```bash
# 查看端口占用
lsof -i :8000  # 后端端口
lsof -i :3000  # 前端端口
lsof -i :27017 # MongoDB 端口
lsof -i :6379  # Redis 端口

# 终止占用进程
kill -9 <PID>
```

### 3. 数据库连接失败
```bash
# 检查 Docker 容器状态
docker-compose ps

# 重启数据库
docker-compose restart mongodb redis

# 查看详细日志
docker-compose logs mongodb
docker-compose logs redis
```

### 4. 后端启动报错
- 检查虚拟环境是否激活：`which python`
- 检查依赖是否安装：`pip list | grep fastapi`
- 检查 .env 文件配置：`cat .env`

### 5. 前端无法连接后端
- 确认后端已启动：访问 http://localhost:8000/api/health
- 检查 CORS 配置：确认 `.env` 中的 `CORS_ORIGINS` 包含前端地址
- 清除浏览器缓存

## 📝 配置说明

### 环境变量配置 (.env)

主要配置项已自动设置，如需自定义：

```bash
# 编辑配置文件
vi .env
```

**关键配置：**
- `MONGODB_HOST` / `REDIS_HOST`：数据库地址（Docker 环境使用 localhost）
- `DEEPSEEK_API_KEY`：DeepSeek API 密钥（用于 AI 分析）
- `TUSHARE_TOKEN`：Tushare Token（用于专业 A 股数据）
- `DEBUG`：开发模式，设置为 `true`

### 获取 API 密钥

#### DeepSeek API（推荐，性价比高）
1. 访问：https://platform.deepseek.com/
2. 注册账号
3. 创建 API Key
4. 复制密钥到 `.env` 文件的 `DEEPSEEK_API_KEY`
5. 设置 `DEEPSEEK_ENABLED=true`

#### Tushare Token（可选）
1. 访问：https://tushare.pro/register?reg=tacn
2. 注册并验证邮箱
3. 获取 Token
4. 复制到 `.env` 文件的 `TUSHARE_TOKEN`
5. 设置 `TUSHARE_ENABLED=true`

## 📚 更多资源

- 项目文档：`docs/` 目录
- API 文档：http://localhost:8000/docs（启动后端后访问）
- GitHub Issues：https://github.com/hsliuping/TradingAgents-CN/issues
- 微信公众号：TradingAgents-CN

## ⚠️ 重要提醒

1. **首次部署必须执行数据库初始化脚本**
   ```bash
   python scripts/import_config_and_create_user.py --host
   ```

2. **默认登录信息**
   - 用户名：`admin`
   - 密码：`admin123`
   - 首次登录后请立即修改密码

3. **开发环境配置**
   - `.env` 文件已设置 `DEBUG=true`
   - 生产环境部署时请修改相关安全配置

4. **数据备份**
   - 定期备份 MongoDB 数据
   - ```bash
     docker exec tradingagents-mongodb mongodump --db tradingagents --out /backup
     docker cp tradingagents-mongodb:/backup ./backup
     ```

---

**祝您使用愉快！** 🎉

如有问题，请查看 `docs/` 目录下的详细文档或提交 Issue。
